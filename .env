# Defaults seguros (sin localhost).
# Para desarrollo local crea .env.local con los valores de tu máquina.
# .env.local ya está en .gitignore (*.local) y nunca se sube a producción.

VITE_API_BASE_URL=
VITE_INTERNOS_BASE_URL=
